import { Ionicons } from '@expo/vector-icons';
import { AudioManager, AudioRecorder, FileFormat, FilePreset } from 'react-native-audio-api';
import { router } from 'expo-router';
import React, { useContext, useEffect, useRef, useState } from 'react';
import { Image, ImageBackground, Platform, Pressable, SafeAreaView, Text, View } from 'react-native';
import BackIcon from '../assets/svg/back-icon.svg';
import { AuthContext } from '../context/AuthContext';
import expoconfig from '../expoconfig';
import styles from '../styles/stylesQuackTalkPracticeRoom';
import { ConversationEvaluation, GeminiGuidedPhraseLive, GuidedLiveAccess } from '../services/guidedPhraseLive';

const background=require('../assets/quacktalk/talk-with-sumi-lounge-v1.png');
const idle=require('../assets/img/Sumi_PoseB_WinterUni_Smile.png');
const blink=require('../assets/img/Sumi_PoseB_WinterUni_EyesClosed_Smile.png');
const speaking=require('../assets/img/Sumi_PoseB_WinterUni_Open.png');
const speakingBlink=require('../assets/img/Sumi_PoseB_WinterUni_EyesClosed_Open.png');
const listening=require('../assets/img/Sumi_PoseB_WinterUni_Smile_Blush.png');
type Scores={speechDetected?:boolean;recognizedText?:string;pronunciation:number;accuracy:number;fluency:number;completeness:number};
async function request(url:string,options:RequestInit={}){
 const controller=new AbortController();const timeout=setTimeout(()=>controller.abort(),45000);
 try{return await fetch(url,{...options,signal:controller.signal});}finally{clearTimeout(timeout);}
}
type State='connecting'|'thinking'|'start'|'speaking'|'ready'|'listening'|'saving'|'finished'|'error';

export default function TalkWithSumiLiveRoom(){
 const transmitting=useRef(false);const mounted=useRef(true);const ending=useRef(false);const pending=useRef<Promise<void>[]>([]);const countRef=useRef(0);const sessionId=useRef('talk-'+Date.now().toString(36)+'-'+Math.random().toString(36).slice(2));const [remaining,setRemaining]=useState(10);const [resetAt,setResetAt]=useState('');const [clock,setClock]=useState(Date.now());const recordingTimeout=useRef<ReturnType<typeof setTimeout>|null>(null);
 const {user}=useContext(AuthContext);const liveRef=useRef<GeminiGuidedPhraseLive|null>(null);const recorderRef=useRef<AudioRecorder|null>(null);const webStreamRef=useRef<any>(null);const webContextRef=useRef<any>(null);const webProcessorRef=useRef<any>(null);const webRecorderRef=useRef<any>(null);const chunksRef=useRef<Blob[]>([]);const scoresRef=useRef<Scores[]>([]);const evaluationsRef=useRef<ConversationEvaluation[]>([]);const transcriptsRef=useRef<string[]>([]);const startedAt=useRef(Date.now());const micBusy=useRef(false);const [state,setState]=useState<State>('connecting');const [message,setMessage]=useState('Opening the conversation lounge…');const [blinked,setBlinked]=useState(false);const [mouth,setMouth]=useState(false);const [turns,setTurns]=useState(0);const [saveError,setSaveError]=useState('');
 const frame=state==='speaking'?(blinked?speakingBlink:mouth?speaking:idle):state==='listening'?listening:(blinked?blink:idle);
 useEffect(()=>{const id=setInterval(()=>{setBlinked(true);setTimeout(()=>setBlinked(false),150);},3500);return()=>clearInterval(id);},[]);
 useEffect(()=>{if(state!=='speaking'){setMouth(false);return;}const id=setInterval(()=>setMouth(value=>!value),170);return()=>clearInterval(id);},[state]);
 useEffect(()=>{if(!user?.email){setState('error');setMessage('Sign in to talk with Sumi.');return;}let active=true;mounted.current=true;const start=async()=>{try{const response=await request(`${expoconfig.API_URL}/api/talk-with-sumi/live-token`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:user.email})});const body=await response.json();if(body.resetsAt)setResetAt(body.resetsAt);if(typeof body.remaining==='number')setRemaining(body.remaining);if(!response.ok)throw new Error(body.message||'The conversation could not open.');if(!active)return;if(Platform.OS!=='web'){AudioManager.setAudioSessionOptions({iosCategory:'playback',iosMode:'spokenAudio',iosOptions:['allowBluetoothA2DP']});await AudioManager.setAudioSessionActivity(true);}const live=new GeminiGuidedPhraseLive({onConnected:()=>{if(!active)return;setState(Platform.OS==='web'?'start':'thinking');setMessage(Platform.OS==='web'?'Tap Start conversation so Sumi can greet you.':'Sumi is greeting you…');},onSpeaking:value=>{if(!active||ending.current)return;if(value){setState('speaking');setMessage('Listen to Sumi…');}else {setState(current=>current==='start'||current==='error'||current==='listening'||current==='saving'||current==='finished'?current:'ready');setMessage('Your turn—tap the microphone and answer naturally.');}},onInputTranscript:text=>{const clean=text.trim();if(clean)transcriptsRef.current=[...transcriptsRef.current,clean].slice(-30);},onOutputTranscript:()=>undefined,onTurn:()=>undefined,onEvaluation:()=>undefined,onConversationEvaluation:value=>{evaluationsRef.current=[...evaluationsRef.current,value];},onError:text=>{if(!active||ending.current)return;setState('error');setMessage(text);},onComplete:()=>{setState('error');setMessage('The live conversation ended. Save your session and return to QuackTalk.');}},'conversation');liveRef.current=live;await live.connect(body as GuidedLiveAccess);}catch(error){setState('error');setMessage(error instanceof Error?error.message:'The conversation could not open.');}};void start();return()=>{active=false;mounted.current=false;transmitting.current=false;if(recordingTimeout.current)clearTimeout(recordingTimeout.current);liveRef.current?.close();void recorderRef.current?.stop();webStreamRef.current?.getTracks?.().forEach((track:any)=>track.stop());void webContextRef.current?.close?.();};},[user?.email]);

 const unlock=async()=>{try{setState('thinking');await liveRef.current?.activateAudio();setMessage('Sumi is greeting you…');}catch(error){setState('start');setMessage(error instanceof Error?error.message:'Tap again to start the conversation.');}};
 const startWeb=async()=>{const devices=(globalThis.navigator as any)?.mediaDevices;if(!devices?.getUserMedia)throw new Error('Microphone recording is unavailable in this browser.');const stream=await devices.getUserMedia({audio:{channelCount:1,echoCancellation:true,noiseSuppression:true,autoGainControl:true}});const AC=(globalThis as any).AudioContext||(globalThis as any).webkitAudioContext;const context=new AC();await context.resume();const source=context.createMediaStreamSource(stream);const processor=context.createScriptProcessor(2048,1,1);const silent=context.createGain();silent.gain.value=0;processor.onaudioprocess=(event:any)=>{const input=event.inputBuffer.getChannelData(0);const ratio=context.sampleRate/16000;const output=new Float32Array(Math.max(1,Math.floor(input.length/ratio)));for(let i=0;i<output.length;i++)output[i]=input[Math.min(input.length-1,Math.floor(i*ratio))];if(transmitting.current)liveRef.current?.sendPcm16(output);};source.connect(processor);processor.connect(silent);silent.connect(context.destination);const MR=(globalThis as any).MediaRecorder;if(!MR)throw new Error('This browser cannot prepare speech review.');const recorder=new MR(stream);chunksRef.current=[];recorder.ondataavailable=(event:any)=>{if(event.data?.size)chunksRef.current.push(event.data);};webStreamRef.current=stream;webContextRef.current=context;webProcessorRef.current=processor;webRecorderRef.current=recorder;recorder.start(250);};
 const startMic=async()=>{if(state!=='ready'||micBusy.current||ending.current||remaining<=0)return;micBusy.current=true;try{await liveRef.current?.activateAudio();liveRef.current?.interrupt();if(Platform.OS==='web')await startWeb();else{const permission=await AudioManager.requestRecordingPermissions();if(permission!=='Granted')throw new Error('Microphone permission is required.');AudioManager.setAudioSessionOptions({iosCategory:'playAndRecord',iosMode:'default',iosOptions:['defaultToSpeaker','allowBluetoothHFP']});await AudioManager.setAudioSessionActivity(true);const recorder=new AudioRecorder();recorder.enableFileOutput({format:FileFormat.Wav,preset:FilePreset.Medium});recorder.onAudioReady({sampleRate:16000,bufferLength:640,channelCount:1},({buffer})=>{if(transmitting.current)liveRef.current?.sendPcm16(buffer.getChannelData(0));});recorderRef.current=recorder;const result=await recorder.start();if(result.status==='error')throw new Error(result.message);}const reservation=await request(`${expoconfig.API_URL}/api/talk-with-sumi/reserve-response`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:user?.email})});const quota=await reservation.json();if(quota.resetsAt)setResetAt(quota.resetsAt);if(typeof quota.remaining==='number')setRemaining(quota.remaining);if(!reservation.ok)throw new Error(quota.message||'Your response could not start.');
 liveRef.current?.beginUserAudio();transmitting.current=true;setState('listening');setMessage('Sumi is listening. Speak naturally, then tap Stop.');}catch(error){webProcessorRef.current?.disconnect?.();webStreamRef.current?.getTracks?.().forEach((track:any)=>track.stop());void webContextRef.current?.close?.();if(recorderRef.current){recorderRef.current.clearOnAudioReady();await recorderRef.current.stop().catch(()=>undefined);recorderRef.current=null;}liveRef.current?.endUserAudio();setState('ready');setMessage(error instanceof Error?error.message:'The microphone could not start.');}finally{micBusy.current=false;}};
 const stopMic=async()=>{if(state!=='listening'||micBusy.current)return;micBusy.current=true;transmitting.current=false;if(recordingTimeout.current)clearTimeout(recordingTimeout.current);try{let audio:string|Blob|null=null;if(Platform.OS==='web'){const recorder=webRecorderRef.current;webRecorderRef.current=null;if(recorder){audio=await new Promise<Blob>((resolve,reject)=>{recorder.onerror=()=>reject(new Error('Recording could not finish.'));recorder.onstop=()=>resolve(new Blob(chunksRef.current,{type:recorder.mimeType||'audio/webm'}));recorder.stop();});}webProcessorRef.current?.disconnect?.();webStreamRef.current?.getTracks?.().forEach((track:any)=>track.stop());await webContextRef.current?.close?.();webProcessorRef.current=null;webStreamRef.current=null;webContextRef.current=null;}else{const recorder=recorderRef.current;recorderRef.current=null;if(recorder){recorder.clearOnAudioReady();const result=await recorder.stop();if(result.status==='success'&&result.paths[0])audio=result.paths[0];}AudioManager.setAudioSessionOptions({iosCategory:'playback',iosMode:'spokenAudio',iosOptions:['allowBluetoothA2DP']});await AudioManager.setAudioSessionActivity(true).catch(()=>undefined);}await liveRef.current?.activateAudio();liveRef.current?.endUserAudio();countRef.current+=1;setTurns(countRef.current);setState('thinking');setMessage('Sumi is thinking about your answer…');if(audio){const review=assess(audio);pending.current.push(review);}}catch(error){setState('ready');setMessage(error instanceof Error?error.message:'Please try your response again.');}finally{micBusy.current=false;}};
 const assess=async(audio:string|Blob)=>{try{const form=new FormData();if(typeof audio==='string')form.append('audio',{uri:audio.startsWith('file:')?audio:`file://${audio}`,name:'conversation-turn.wav',type:'audio/wav'} as any);else form.append('audio',audio,'conversation-turn.webm');const response=await request(`${expoconfig.API_URL}/api/talk-with-sumi/assess`,{method:'POST',body:form});const body=await response.json();if(!response.ok)throw new Error('One response could not be assessed.');if(body.speechDetected!==false)scoresRef.current=[...scoresRef.current,body];}catch{if(mounted.current)setSaveError('Some speech feedback is unavailable. Only successful assessments will contribute to your score.');}};
const finish=async()=>{if(ending.current||state==='finished'||micBusy.current)return;if(state==='listening'){await stopMic();}ending.current=true;setState('saving');setMessage('Saving your conversation feedback…');liveRef.current?.close();await Promise.allSettled(pending.current);const scores=scoresRef.current;const evaluations=evaluationsRef.current;const avg=(key:keyof Scores)=>scores.length?Math.round(scores.reduce((sum,item)=>sum+Number(item[key]||0),0)/scores.length):0;const pronunciation=avg('pronunciation'),accuracy=avg('accuracy'),fluency=avg('fluency'),completeness=avg('completeness');const contextual=evaluations.length?Math.round(evaluations.reduce((sum,item)=>sum+Number(item.contextScore||0),0)/evaluations.length):0;const overall=Math.round(((pronunciation+accuracy+fluency+completeness)/4+contextual)/2);const areas=[...new Set(evaluations.map(item=>item.improvementArea).filter(Boolean))].slice(0,8);try{const response=await request(`${expoconfig.API_URL}/api/talk-with-sumi/complete`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({sessionId:sessionId.current,email:user?.email,name:`${user?.fname??''} ${user?.lname??''}`.trim(),durationSeconds:Math.max(1,Math.round((Date.now()-startedAt.current)/1000)),conversationTurns:countRef.current,score:overall,pronunciationScore:pronunciation,accuracyScore:accuracy,fluencyScore:fluency,completenessScore:completeness,contextualAccuracy:contextual,expressionsPracticed:transcriptsRef.current,areasForImprovement:areas,turnAssessments:scores,feedbackSummary:evaluations.slice(-3).map(item=>item.assessment).filter(Boolean).join(' ')||'Keep building confidence through short, natural Japanese exchanges.'})});if(!response.ok)throw new Error('Your feedback could not be saved.');setState('finished');setMessage('Your conversation and feedback are saved.');}catch(error){ending.current=false;setSaveError(error instanceof Error?error.message:'Your feedback could not be saved.');setState('error');}};
 const leave=async()=>{if((countRef.current>0||state==='listening')&&state!=='finished')await finish();else router.replace('/QuackTalk');};
 useEffect(()=>{const timer=setInterval(()=>setClock(Date.now()),1000);return()=>clearInterval(timer);},[]);
 useEffect(()=>{if(state!=='listening')return;recordingTimeout.current=setTimeout(()=>void stopMic(),25000);return()=>{if(recordingTimeout.current)clearTimeout(recordingTimeout.current);};},[state]);
 useEffect(()=>{if(remaining===0&&state==='ready'&&countRef.current>0)void finish();},[remaining,state]);
 const unlockSeconds=Math.max(0,Math.ceil((new Date(resetAt).getTime()-clock)/1000));
 return <SafeAreaView style={styles.safeArea}><ImageBackground source={background} style={[styles.background,{backgroundColor:'#251A35'}]} imageStyle={styles.backgroundArtwork} resizeMode="cover"><View style={styles.sceneTint}/><View style={styles.header}><Pressable onPress={()=>void leave()} style={styles.headerButton}><BackIcon width={18} height={18} fill="#47295A"/></Pressable><View style={[styles.headerMark,{backgroundColor:'#F0E7FA'}]}><Ionicons name="chatbubbles" size={19} color="#7552C8"/></View><View style={styles.headerCopy}><Text style={[styles.headerEyebrow,{color:'#7552C8'}]}>CONVERSATION LOUNGE</Text><Text style={styles.headerTitle}>Talk with Sumi</Text></View><Pressable onPress={()=>{if(countRef.current>0||state==='listening')void finish();else{liveRef.current?.close();router.push({pathname:'/QuackTalkFeedback',params:{returnTo:'conversation'}});}}} style={[styles.headerButton,{backgroundColor:'#F0E7FA'}]}><Ionicons name="analytics-outline" size={21} color="#7552C8"/></Pressable></View><View style={styles.stage}><View style={styles.coachStatus}><View style={styles.onlineDot}/><View><Text style={styles.coachStatusText}>{state==='listening'?'SUMI IS LISTENING':state==='speaking'?'SUMI IS SPEAKING':'OPEN CONVERSATION'}</Text><Text style={styles.coachStatusSubtext}>{remaining} of 10 responses remaining today</Text></View></View><Image source={frame} style={styles.sumi} resizeMode="contain"/><View style={styles.controlPanel}><View style={styles.guidedPromptCard}><View style={styles.guidedPromptTop}><View style={[styles.promptIcon,{backgroundColor:'#F0E7FA'}]}><Ionicons name={state==='listening'?'mic':state==='speaking'?'volume-high':'chatbubbles'} size={20} color="#7552C8"/></View><View style={styles.promptCopy}><Text style={[styles.promptLabel,{color:'#7552C8'}]}>VOICE-TO-VOICE JAPANESE</Text><Text style={styles.promptTitle}>{message}</Text></View></View><Text style={styles.guidedInstruction}>There is no transcript here—listen, respond naturally, and ask for English help whenever you need it.</Text></View>{state==='start'?<Pressable onPress={()=>void unlock()} style={[styles.tutorialDone,{backgroundColor:'#7552C8'}]}><Text style={styles.tutorialDoneText}>Start conversation</Text></Pressable>:state==='finished'?<View style={styles.sessionCompleteActions}><Pressable style={styles.sessionCompleteSecondary} onPress={()=>router.replace('/QuackTalk')}><Text style={styles.sessionCompleteSecondaryText}>Back to QuackTalk</Text></Pressable><Pressable style={styles.sessionCompletePrimary} onPress={()=>router.push({pathname:'/QuackTalkFeedback',params:{returnTo:'conversation'}})}><Text style={styles.sessionCompletePrimaryText}>View feedback</Text></Pressable></View>:<View style={styles.learnerMicCard}><View style={styles.learnerMicCopy}><Text style={styles.learnerMicEyebrow}>{state==='listening'?'YOUR TURN':'CONVERSATION CONTROL'}</Text><Text style={styles.learnerMicTitle}>{state==='listening'?'Speak to Sumi now':state==='ready'?'Ready for your response':state==='speaking'?'Listen to Sumi':'Conversation status'}</Text><Text style={styles.microphoneStatus}>{state==='ready'?'Tap the microphone when Sumi finishes.':state==='listening'?'Tap Stop when your response is complete.':'The microphone unlocks when it is your turn.'}</Text></View><Pressable disabled={(state!=='ready'&&state!=='listening')||(remaining===0&&state!=='listening')} onPress={()=>void(state==='listening'?stopMic():startMic())} style={[styles.microphoneButton,{backgroundColor:state==='listening'?'#E34F6C':'#7552C8',opacity:state==='ready'||state==='listening'?1:.45}]}><Ionicons name={state==='listening'?'stop':'mic'} size={26} color="#FFFFFF"/></Pressable></View>} {turns>0&&state!=='finished'?<Pressable onPress={()=>void finish()} style={[styles.headerButton,{width:'100%',height:44,marginTop:10}]}><Ionicons name="stop-circle-outline" size={18} color="#7552C8"/><Text style={{fontFamily:'Jua',fontSize:12,color:'#7552C8'}}>End and save conversation</Text></Pressable>:null}{remaining===0&&resetAt?<Text style={styles.assessmentMeaning}>Today's responses are used. Available again in {Math.floor(unlockSeconds/3600)}h {Math.floor(unlockSeconds%3600/60)}m.</Text>:null}{saveError?<Text style={styles.assessmentMeaning}>{saveError}</Text>:null}</View></View></ImageBackground></SafeAreaView>;
}
